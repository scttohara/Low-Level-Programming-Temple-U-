#include<stdio.h>
//#include "conio2.h"

int main(int argc, char *argv[ ]) {

    // Declare variables
    string name;
    int age = 0;

    // Print Hello World!
    printf("Hello World!");

    //Get user name
    printf("\nEnter your name: ");
    scanf("%s", &name);

    //Get user age
    printf("\nEnter your age");
    scanf("%s", &age);

    //Print data to screen
    printf("\nHello %s your age is %d\n", name, age);

    printf("\nPress a ket to exit");
    getch();
    return 0;

    }
